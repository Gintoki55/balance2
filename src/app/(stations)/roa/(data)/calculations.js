// // calculations.js
// export const runCalculation = (data) => {
//   if (!Array.isArray(data)) return []; // تأكد أن فيه بيانات

//   return data.map(row =>
//     row.map(cell => ({
//       ...cell,
//       value: typeof cell.value === "number" ? cell.value + 10 : cell.value
//     }))
//   );
// };
